# Agent Memory Playground

Local development UI for trying the SDK without creating an app first.

```bash
pnpm --filter @agent-memory/playground dev
```

Then open:

```txt
http://localhost:4317
```

The playground is private and is not included in npm package artifacts.

